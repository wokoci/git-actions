name: another workflow

on: 
    - push
    - workflow_dispatch

jobs:
    greet_me:
        runs-on: ubuntu-latest
        steps:
            - name: show time
              run: echo "Hello, the date is $(date)"

       



    
